// Centralized tunable parameters of the popup (pagination sizes etc.);
// future parameters of this kind belong here too. The backend counterpart is
// backend/tab_server_params.py.

// one page of remote search results (/api/search)
export const REMOTE_SEARCH_PAGE_SIZE = 100

// one page of the remote tag selector's dropdown list
// (/api/tabTag/list and /api/tabTag/search)
export const REMOTE_TAG_PAGE_SIZE = 50
